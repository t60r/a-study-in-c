#include <stdio.h>
#include <ctype.h>

/* reverses case of phrase[] */
/* ***Horribly Incomplete*** */

int main() {
	char phrase[] = "The rain it raineth on the just And also on the unjust fella. But chiefly on the just, because The unjust steals the just's umbrella.";

	int index,lower,upper,s;

	lower = upper = s = 0;
	/* Print results */
	printf("\"%s\"\n", phrase);

	return(0);
}

